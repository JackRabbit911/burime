<p class="text-medium">
    {{ __('First of all,') }}
    <a
        href="{{ path('auth', {action: 'form'}) }}"
        class="link text-primary"
    >
    {{ __('You need to log in') }}
    </a>
    {{ __('to our website') }}
</p>
