<div class="flex flex-row flex-wrap justify-between items-center h-auto">
  <h1 class="text-2xl md:text-3xl">{{ title }}</h1>
  <div class="hidden md:block">
    {{ paginator|raw }}
  </div>
  {{ switcher|raw }}
</div>
<div class="flex flex-row flex-wrap justify-center items-center h-auto md:hidden">
  {{ paginator|raw }}
</div>
