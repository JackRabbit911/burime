{% extends 'common/container.twig' %}
{% block content %}
{{ include('home/common/header.twig') }}
<div class="overflow-x-auto">
  <table class="table table-hover table-zebra">
    <thead>
      <tr>
        <th>{{ __('Author') }}</th>
        <th>{{ __('Title') }}</th>
        <th>{{ __('Genre') }}</th>
      </tr>
    </thead>
    {{ content }}
  </table>
</div>
{% endblock content %}
