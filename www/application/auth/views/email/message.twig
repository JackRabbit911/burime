<h2>{{ __(title) }}</h2>
<div>
    {{ app.cmp('ImgInline', {'path':'writers.jpg', 'alt':'Burime', 'height':'150'})|raw }}
</div>
<p>{{ __(appeal, {username: username}) }}</p>
<p>
    {{ __(msg) }}
    <a href="{{ __(link.href, {code: app.session.code}) }}">
        {{ __(link.title) }}
    </a>
</p>
