{{ __('I agree with the') }} 
<a href="{{ '#' }}" class="link font-semibold">{{ __('site rules') }}</a> 
{{ __('and') }} 
<a href="{{ '#' }}" class="link font-semibold">{{ __('privacy policy') }}</a>
