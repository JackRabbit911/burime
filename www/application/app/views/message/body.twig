<p class="text-base-content/75 py-2">{{ __('Subject') }}</p>
<p class="border border-base-300 text-base-content rounded p-3">{{ msg.subject }}</p>
<p class="text-base-content/75 py-2">{{ __('Message') }}</p>
<div class="border border-base-300 text-base-content rounded p-3">
    {{ include(msg.view ~ '.twig') }}
</div>
