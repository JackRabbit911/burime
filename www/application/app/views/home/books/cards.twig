{% extends 'common/layout.twig' %}
{% block content %}
<div class="grid md:grid-cols-4 xl:grid-cols-6 gap-4 mt-4 h-auto">
    {% for book in books %}
    <a href="{{ path('branch', {'branch_id': book.id}) }}">
    {{ b.book(book) }}
    </a>
    {% endfor %}
</div>
{% endblock %}
