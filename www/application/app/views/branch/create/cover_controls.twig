<div class="flex flex-row justify-between mt-4">
    <button form="{{ form.form.id }}" name="sbmt" type="submit" class="btn btn-neutral" value="authors">
        {{ __('Prev') }}
    </button>
    <div>
        <button form="{{ form.form.id }}" name="sbmt" type="submit" class="btn" value="reset">
            {{ __('Reset') }}
        </button>
        <button form="{{ form.form.id }}" name="sbmt" type="submit" class="btn" value="cover">
            {{ __('View result') }}
        </button>
    </div>
    <button form="{{ form.form.id }}" name="sbmt" type="submit" class="btn btn-primary" value="publish">
        {{ __('Next') }}
    </button>
</div>
